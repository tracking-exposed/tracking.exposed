when:
  event: push
  branch:
    - master
    
pipeline:
  build:
    image: klakegg/hugo:ext-alpine-ci
    commands:
      - hugo
      - rm -rf /site/*
      - mv -f public/* /site
    volumes:
      - /var/www/sites/tracking.exposed:/site
